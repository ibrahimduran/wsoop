{
  "name": "wsoop",
  "version": "0.1.2",
  "description": "Framework for developing effecient WebSocket server applications.",
  "main": "build/index.js",
  "types": "./build/index.d.ts",
  "typescript.tsdk": "./build",
  "scripts": {
    "build": "tsc",
    "dev-env": "tsc --watch",
    "test": "istanbul cover -x test/**/* node_modules/mocha/bin/_mocha -- --compilers ts:ts-node/register -R spec test/**/*",
    "remap-coverage": "remap-istanbul -i coverage/coverage.json -o coverage/remapped -t html -b build",
    "coveralls": "istanbul cover -x test/**/* node_modules/mocha/bin/_mocha --report lcovonly -- --compilers ts:ts-node/register -R spec test/**/* && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/ibrahimduran/wsoop.git"
  },
  "keywords": [
    "socket",
    "websocket",
    "framework"
  ],
  "author": "İbrahim Duran",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ibrahimduran/wsoop/issues"
  },
  "homepage": "https://github.com/ibrahimduran/wsoop#readme",
  "devDependencies": {
    "@types/chai": "^3.4.35",
    "@types/mocha": "^2.2.39",
    "chai": "^3.5.0",
    "coveralls": "^2.12.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "remap-istanbul": "^0.9.1",
    "socket.io-client": "^1.7.3",
    "ts-node": "^2.1.0",
    "typescript": "^2.2.1"
  },
  "dependencies": {
    "@types/socket.io": "^1.4.29",
    "debug": "^2.6.1",
    "socket.io": "^1.7.3"
  }
}
